@model IEnumerable<CDMS.Entities.AGEGROUP>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.agegroupname)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.agegroupname)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.agegroupid }) |
            @Html.ActionLink("Details", "Details", new { id=item.agegroupid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.agegroupid })
        </td>
    </tr>
}

</table>
